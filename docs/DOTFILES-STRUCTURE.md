# Dotfiles Structure

```text
dotfiles/skel/
  .config/
    kdeglobals
    plasma-org.kde.plasma.desktop-appletsrc
    konsolerc
  .local/share/
    konsole/
      CatCafe.profile
```

These files seed the default user environment and may be copied into `/etc/skel` during image creation.
